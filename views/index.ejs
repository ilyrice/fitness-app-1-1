<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>แอปออกกำลังกายง่ายๆ</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>Easy Workout</h1>
    </header>
    
    <main>
        <div class="exercise-list">
            <% exercises.forEach(exercise => { %>
                <div class="exercise-card">
                    <img src="<%= exercise.image %>" alt="<%= exercise.name %>">
                    <h2><%= exercise.name %></h2>
                    <p><%= exercise.description %></p>
                    <p><strong><%= exercise.reps %></strong></p>
                    <a href="/exercise/<%= exercise.id %>" class="btn">ดูรายละเอียด</a>
                </div>
            <% }); %>
        </div>
    </main>
    
    <footer>
        <p>สร้างด้วย Node.js และ CSS</p>
    </footer>
</body>
</html>